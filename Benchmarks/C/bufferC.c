#include <stdio.h>
#include <string.h>

int main() {
    const int N=  10;    
    char buffer[N];
    strcpy(buffer, "Soft trends :))))))");
    //gets(buffer);
    

    printf("Buffer = %s\n", buffer);


    return 0;
}


